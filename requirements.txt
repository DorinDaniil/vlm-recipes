# Базовое — нужно всегда.
# torch здесь намеренно НЕ указан: его версия зависит от драйвера вашей
# машины, и pip не должен решать это за вас. См. README, раздел «Установка».
transformers>=4.57
accelerate>=1.0
pillow>=10.0
huggingface_hub>=0.26

# Обучение (vlmkit.train). Без них инференс и гардрейлы работают.
peft>=0.14
trl>=0.13
datasets>=3.0
bitsandbytes>=0.45   # только для 4-битной загрузки (QLoRA)
