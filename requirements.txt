# torch is deliberately absent: its build depends on the driver on your
# machine, and pip should not decide that for you. See the README.
transformers>=4.57
accelerate>=1.0
peft>=0.14
trl>=0.20
datasets>=3.0
matplotlib>=3.8
