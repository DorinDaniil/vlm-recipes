# torch здесь намеренно НЕ указан: его версия зависит от драйвера вашей
# машины, и pip не должен решать это за вас. См. README, раздел «Установка».
transformers>=4.57
accelerate>=1.0
peft>=0.14
trl>=0.13
datasets>=3.0
pillow>=10.0          # картинки в примерах, если добавите сканы страниц
huggingface_hub>=0.26  # tools/check_model.py смотрит карточку модели
